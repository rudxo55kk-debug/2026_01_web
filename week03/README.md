# 세번째 주 웹프로그래밍 강의/실습

## 2026-03-18

1. HTML 태그 실습
   * HTML5의 기본 구조와 자주 쓰는 태그 활용
   * CSS나 JavaScript는 최소한 사용(사용하지 않음)

## 2026-03-19

1. CSS 기본/레이아웃 실습
   * `css_basic.html` : 다양한 셀렉터(태그/클래스/ID/어트리뷰트/하위·자식·형제)와 텍스트 스타일 예제
   * `bax_model.html` : 박스 모델(content/padding/border/margin), display 속성(block/inline/inline-block/flex/grid), position(static/relative/absolute/fixed) 비교
   * `flxe_display.html` : flex 레이아웃 비교 (div 기반 vs 시맨틱 태그 기반)
   * `from_list_table.html` + `form_list_table.css` : 리스트/테이블/폼 스타일 예제 (커스텀 마커, 테이블 행 구분, 현대적 폼 디자인)
   * `css_animation.html` : transition (:hover)과 @keyframes animation 예제
   * `bootstrap_example.html` : Bootstrap CDN 기반 반응형 레이아웃(그리드, navbar, 카드, 폼, 버튼)
